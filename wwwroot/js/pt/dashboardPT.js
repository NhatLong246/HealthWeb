// Dashboard PT - JavaScript chỉ dùng cho hiệu ứng UI
// Dữ liệu được load từ server bằng Razor

document.addEventListener('DOMContentLoaded', function() {
    // Không cần load dữ liệu nữa vì đã được render từ server
    // Chỉ cần xử lý các hiệu ứng UI nếu có
});
